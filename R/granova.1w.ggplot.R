granova.1w.ggplot <- function( data  = NULL,
                               group = NULL,
                               resid = TRUE
                     ) 
{
  convertToDataFrame <- function () {
    if (is.data.frame(data)) {
      return(data)  
    }
    
    if (is.matrix(data))
  }

}

